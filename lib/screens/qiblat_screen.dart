import 'package:flutter/material.dart';

class QiblatScreen extends StatelessWidget {
  const QiblatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Qiblat Screen'),
      ),
    );
  }
}