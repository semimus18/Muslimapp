import 'package:flutter/material.dart';

class DhikirScreen extends StatelessWidget {
  const DhikirScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dhikir Screen'),
      ),
    );
  }
}